services:
  app:
    build: .
    mem_limit: 512m
    cpus: 0.5
    ports:
      - "${PORT}:3000"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: "512M"
        reservations:
          cpus: "0.25"
          memory: "256M"
